<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.nait.rcharitra1.todoornot">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodoOrNot">
        <activity android:name=".ViewArchivedItems"></activity>
        <activity android:name=".EditTodoItemActivity" />
        <activity android:name=".ViewTodoList" />
        <activity android:name=".ViewList" />
        <activity android:name=".PrefsActivity" />
        <activity android:name=".CreateTodoListItemActivity" />
        <activity android:name=".CreateTodoListActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>